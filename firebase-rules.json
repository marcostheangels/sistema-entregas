{
  "rules": {
    "empresas": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "entregadores": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        "empresasBloqueadas": {
          "$empresaId": {
            ".write": "auth != null && auth.uid === $empresaId"
          }
        },
        "lastHeartbeat": {
          ".write": true,
          ".validate": "newData.isNumber()"
        }
      }
    },
    "entregas": {
      ".read": "auth != null",
      ".indexOn": ["empresaId", "status", "entregadorId"],
      "$id": {
        ".write": "auth != null && ((!data.exists() && newData.child('empresaId').val() === auth.uid) || (data.exists() && data.child('empresaId').val() === auth.uid) || (data.exists() && data.child('entregadorId').val() === auth.uid) || (data.exists() && data.child('status').val() === 'pendente' && newData.child('entregadorId').val() === auth.uid))"
      }
    },
    "posicoes": {
      "$uid": {
        ".read": "auth != null",
        ".write": true,
        ".validate": "newData.hasChildren(['lat', 'lng', 'timestamp', 'online']) && newData.child('lat').val() is number && newData.child('lng').val() is number && newData.child('timestamp').val() is number && newData.child('online').val() is boolean"
      }
    }
  }
}
